## 📝 오답노트: 백준 2480번 (주사위 세개)

### 1. 틀린 원인 분석 (Why Wrong?)

**① 문법 오류 (Syntax Error)**
* **`continue` 오사용:** `if-else` 문 안에서는 `continue`를 사용할 수 없음. (반복문 `for`, `while` 내부에서만 건너뛰기 용도로 사용 가능)
* **연산자 실수:** 변수에 값을 넣을 때 `max = a`가 아닌 `max == a`를 사용하여 값이 저장되지 않음. (`==`는 비교 연산자)

**② 논리 오류 (Logical Error)**
* **경우의 수 누락:** 같은 눈이 2개인 경우, `a == b`만 체크하고 `b == c`나 `a == c`인 경우를 놓침.
* **최댓값 로직 복잡화:** 중첩된 `if`문과 잘못된 대입 연산자로 인해 가장 큰 눈을 찾는 로직이 정상 작동하지 않음.

### 2. 해결 방법 (Solution)

* **조건 분기 단순화:** 3개의 주사위 중 2개가 같은 경우는 총 3가지(`a==b`, `a==c`, `b==c`)이므로 이를 빠짐없이 체크해야 함.
* **최댓값 알고리즘:** 복잡한 비교 대신, 임의의 수(a)를 `max`로 두고 나머지 수와 비교하여 갱신하는 방식 사용.

### 3. 배운 점 (Key Takeaways)
* `continue`는 **반복문(Loop)** 제어에만 쓴다.
* 값 대입은 `=`이고, 같다는 비교는 `==`이다. (헷갈리지 말자!)
* `if`문 조건 짤 때 빠진 케이스(Edge Case)가 없는지 한 번 더 확인하자.
